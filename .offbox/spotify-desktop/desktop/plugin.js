/*
 * Hermes for Spotify — Hermes-authored companion renderer.
 * The renderer only consumes the plugin's typed projection API; credential and
 * account lifecycle remain owned by the existing Hermes Spotify integration.
 */
import { host, PALETTE_AREA, ROUTES_AREA, SIDEBAR_NAV_AREA, usePluginI18n } from '@hermes/plugin-sdk'
import { useEffect, useState } from 'react'
import { jsx, jsxs } from 'react/jsx-runtime'

var ID = 'spotify-desktop'
var restRef = null
var osRef = null
var storageRef = null
var AUTH_SETTINGS_DEEP_LINK = 'hermes://open/settings/tools'
var POLL_MS = 5000
var BACKGROUND_POLL_MS = 15000
var QUOTA_POLL_MS = 30000
var STORAGE_KEYS = ['selected_device_id', 'layout']

// Flat authoring table. The SDK resolver walks dot-paths into nested trees
// (i18n/runtime.ts resolvePath), so keys are expanded to nested objects at
// registration below — flat registration renders raw keys (the old bug).
var translationTable = {
  en: {
    'music.title': 'Music', 'music.subtitle': 'Hermes companion for Spotify', 'music.home': 'Home', 'music.search': 'Search', 'music.library': 'Library',
    'connection.checking': 'Checking Spotify connection…', 'connection.login': 'Spotify is not connected.', 'connection.connect': 'Open Hermes auth settings',
    'connection.waiting': 'Waiting for Hermes Spotify authentication to finish…', 'connection.connected': 'Spotify is connected.', 'connection.expired': 'Spotify access needs to be re-authorized.',
    'connection.disconnected': 'Spotify plugin data was cleared on this device.', 'connection.error': 'Spotify connection status is unavailable.', 'connection.retry': 'Retry status',
    'connection.help': 'Run hermes auth spotify in a terminal, then return here.', 'connection.delete': 'Disconnect and delete plugin data',
    'playback.empty': 'Nothing is playing. Open Spotify on a device to begin.', 'playback.free': 'Playback controls require Spotify Premium.', 'playback.restricted': 'This device limits playback controls.',
    'playback.offline': 'Playback is temporarily unavailable.', 'playback.throttled': 'Playback updates are temporarily slowed.', 'playback.open': 'Open in Spotify', 'playback.previous': 'Previous',
    'playback.play': 'Play', 'playback.pause': 'Pause', 'playback.next': 'Next', 'playback.seek': 'Seek', 'playback.attribution': 'Spotify attribution', 'playback.explicit': 'Explicit',
    'shell.comingSoon': 'Choose Home, Search, or Library to continue.', 'shell.disconnectHelp': 'Disconnect clears plugin-owned local data. See DATA_DELETION.md for the five-day deletion procedure.',
    'privacy.title': 'Privacy and personal-use notice', 'privacy.summary': 'This personal, non-commercial companion sends only your explicit text-initiated actions to Spotify through Hermes. It does not provide voice control, in-app audio, analytics, advertising, payments, account registration, or AI/ML ingestion of Spotify content.',
    'privacy.deletion': 'Disconnect removes plugin-owned local state immediately; see PRIVACY_NOTICE.md and DATA_DELETION.md for the deletion procedure.'
  },
  ja: { 'music.title': '音楽', 'music.subtitle': 'Spotify 用 Hermes コンパニオン', 'music.home': 'ホーム', 'music.search': '検索', 'music.library': 'ライブラリ', 'connection.checking': 'Spotify 接続を確認中…', 'connection.login': 'Spotify は接続されていません。', 'connection.connect': 'Hermes 認証設定を開く', 'connection.waiting': 'Hermes Spotify 認証の完了を待機中…', 'connection.connected': 'Spotify は接続されています。', 'connection.expired': 'Spotify の再認証が必要です。', 'connection.disconnected': 'この端末の Spotify プラグインデータを消去しました。', 'connection.error': 'Spotify 接続状態を取得できません。', 'connection.retry': '状態を再試行', 'connection.help': 'ターミナルで hermes auth spotify を実行してから、ここへ戻ってください。', 'connection.delete': '切断してプラグインデータを消去', 'playback.empty': '再生中の項目はありません。Spotify をデバイスで開いてください。', 'playback.free': '再生コントロールには Spotify Premium が必要です。', 'playback.restricted': 'このデバイスでは再生コントロールが制限されています。', 'playback.offline': '再生情報は一時的に利用できません。', 'playback.throttled': '再生情報の更新を一時的に抑制しています。', 'playback.open': 'Spotify で開く', 'playback.previous': '前へ', 'playback.play': '再生', 'playback.pause': '一時停止', 'playback.next': '次へ', 'playback.seek': 'シーク', 'playback.attribution': 'Spotify attribution', 'playback.explicit': '明示的', 'shell.comingSoon': 'ホーム、検索、ライブラリを選択して続行します。', 'shell.disconnectHelp': '切断するとプラグイン所有のローカルデータを消去します。5 日間の削除手順は DATA_DELETION.md を参照してください。', 'privacy.title': 'プライバシーと個人利用に関する通知', 'privacy.summary': 'この個人利用・非商用コンパニオンは、Hermes を介して明示的なテキスト操作のみを Spotify に送信します。音声操作、アプリ内オーディオ、分析、広告、支払い、アカウント登録、Spotify コンテンツの AI/ML 取り込みは提供しません。', 'privacy.deletion': '切断するとプラグイン所有のローカル状態を直ちに削除します。削除手順は PRIVACY_NOTICE.md と DATA_DELETION.md を参照してください。' },
  zh: { 'music.title': '音乐', 'music.subtitle': '面向 Spotify 的 Hermes 伴侣', 'music.home': '主页', 'music.search': '搜索', 'music.library': '媒体库', 'connection.checking': '正在检查 Spotify 连接…', 'connection.login': 'Spotify 尚未连接。', 'connection.connect': '打开 Hermes 认证设置', 'connection.waiting': '正在等待 Hermes Spotify 认证完成…', 'connection.connected': 'Spotify 已连接。', 'connection.expired': 'Spotify 访问需要重新认证。', 'connection.disconnected': '此设备上的 Spotify 插件数据已清除。', 'connection.error': 'Spotify 连接状态不可用。', 'connection.retry': '重试状态', 'connection.help': '请在终端运行 hermes auth spotify，然后返回这里。', 'connection.delete': '断开并删除插件数据', 'playback.empty': '当前没有播放内容。请在设备上打开 Spotify。', 'playback.free': '播放控制需要 Spotify Premium。', 'playback.restricted': '此设备限制播放控制。', 'playback.offline': '播放暂时不可用。', 'playback.throttled': '播放更新暂时已放缓。', 'playback.open': '在 Spotify 中打开', 'playback.previous': '上一首', 'playback.play': '播放', 'playback.pause': '暂停', 'playback.next': '下一首', 'playback.seek': '定位', 'playback.attribution': 'Spotify attribution', 'playback.explicit': '露骨内容', 'shell.comingSoon': '选择主页、搜索或媒体库以继续。', 'shell.disconnectHelp': '断开连接会清除插件拥有的本地数据。请参阅 DATA_DELETION.md 了解五天删除程序。', 'privacy.title': '隐私和个人使用说明', 'privacy.summary': '此个人、非商业伴侣仅通过 Hermes 向 Spotify 发送您明确发起的文本操作。它不提供语音控制、应用内音频、分析、广告、付款、帐户注册或 Spotify 内容的 AI/ML 摄取。', 'privacy.deletion': '断开连接会立即删除插件拥有的本地状态；请参阅 PRIVACY_NOTICE.md 和 DATA_DELETION.md 了解删除程序。' },
  'zh-hant': { 'music.title': '音樂', 'music.subtitle': '適用於 Spotify 的 Hermes 夥伴', 'music.home': '首頁', 'music.search': '搜尋', 'music.library': '媒體庫', 'connection.checking': '正在檢查 Spotify 連線…', 'connection.login': 'Spotify 尚未連線。', 'connection.connect': '開啟 Hermes 驗證設定', 'connection.waiting': '正在等待 Hermes Spotify 驗證完成…', 'connection.connected': 'Spotify 已連線。', 'connection.expired': 'Spotify 存取需要重新驗證。', 'connection.disconnected': '此裝置上的 Spotify 外掛資料已清除。', 'connection.error': 'Spotify 連線狀態不可用。', 'connection.retry': '狀態再試', 'connection.help': '請在終端機執行 hermes auth spotify，然後回到這裡。', 'connection.delete': '中斷並刪除外掛資料', 'playback.empty': '目前沒有播放播放內容。請在裝置上開啟 Spotify。', 'playback.free': '播放控制需要 Spotify Premium。', 'playback.restricted': '此裝置限制播放控制。', 'playback.offline': '播放暫時不可用。', 'playback.throttled': '播放更新暫時已放緩。', 'playback.open': '在 Spotify 中開啟', 'playback.previous': '上一首', 'playback.play': '播放', 'playback.pause': '暫停', 'playback.next': '下一首', 'playback.seek': '定位', 'playback.attribution': 'Spotify attribution', 'playback.explicit': '明確內容', 'shell.comingSoon': '選擇首頁、搜尋或媒體庫以繼續。', 'shell.disconnectHelp': '中斷連線會清除外掛擁有的本機資料。請參閱 DATA_DELETION.md 了解五天刪除程序。', 'privacy.title': '隱私權與個人使用聲明', 'privacy.summary': '此個人、非商業夥伴僅透過 Hermes 將您明確發起的文字操作傳送至 Spotify。它不提供語音控制、應用程式內音訊、分析、廣告、付款、帳戶註冊或 Spotify 內容的 AI/ML 擷取。', 'privacy.deletion': '中斷連線會立即刪除外掛擁有的本機狀態；請參閱 PRIVACY_NOTICE.md 和 DATA_DELETION.md 了解刪除程序。' }
}

// Expand 'a.b.c' keys into nested trees the SDK resolver understands.
function nestMessages(flat) {
  var tree = {}
  Object.keys(flat).forEach(function (key) {
    var parts = key.split('.')
    var node = tree
    for (var i = 0; i < parts.length - 1; i++) {
      if (!node[parts[i]] || typeof node[parts[i]] !== 'object') node[parts[i]] = {}
      node = node[parts[i]]
    }
    node[parts[parts.length - 1]] = flat[key]
  })
  return tree
}
var translations = {}
Object.keys(translationTable).forEach(function (locale) { translations[locale] = nestMessages(translationTable[locale]) })

// SDK contract: usePluginI18n(id) IS the t function (not {t})
function useI18n() { return { t: usePluginI18n(ID) } }
function getStatus() { return restRef ? restRef('/status', { method: 'GET' }) : Promise.reject(new Error('transport unavailable')) }
function getPlayback() { return restRef ? restRef('/playback', { method: 'GET' }) : Promise.reject(new Error('transport unavailable')) }
// SDK contract (kanban api.ts): ctx.rest bodies are plain objects; the Electron
// bridge serializes exactly once (fetchJson JSON.stringify). Pre-stringifying
// here double-encodes and every mutation 422s (P4 F1).
function mutateRest(path, method, body) { if (!restRef) return Promise.reject(new Error('transport unavailable')); return restRef(path, { method: method, body: body || {} }) }
function contextUri(kind, id) { return id ? 'spotify:' + kind + ':' + id : null }
function apiCategory(error) { return error && (error.category || (error.detail && error.detail.category) || (error.response && error.response.category)) }
function connectionState(status, disconnected) { if (disconnected) return 'disconnected'; var value = status && status.auth && status.auth.state; if (value === 'connected' || value === 'credentials_available') return 'connected'; if (value === 'expired' || value === 'revoked' || value === 'reauth_required') return 'expired'; if (value === 'not_authenticated') return 'login_required'; return 'error' }
function transitionConnection(current, event) { if (event === 'disconnect') return 'disconnected'; if (event === 'connect_opened' && (current === 'login_required' || current === 'expired' || current === 'disconnected')) return 'connecting'; if (event === 'status_connected' && current === 'connecting') return 'connected'; if (event === 'status_expired' && current === 'connecting') return 'expired'; if (event === 'status_not_authenticated' && current === 'connecting') return 'login_required'; return current }
function clearPluginData(storage) { if (storage && typeof storage.remove === 'function') STORAGE_KEYS.forEach(function (key) { storage.remove(key) }) }
function interpolateProgress(playback, now) { var item = playback && playback.item; var duration = item && Number(item.duration_ms); var progress = Number(playback && playback.progress_ms) || 0; if (playback && playback.is_playing && Number.isFinite(Number(playback.timestamp))) progress += Math.max(0, now - Number(playback.timestamp)); return Math.max(0, Math.min(Number.isFinite(duration) ? duration : progress, progress)) }
function clampSeek(position, duration) { return Math.max(0, Math.min(Number(duration) || 0, Number(position) || 0)) }
function playbackUiState(value) { var category = value && value.category; if (category === 'premium_required') return 'free_read_only'; if (category === 'no_active_device') return 'no_device'; if (category === 'restricted_device') return 'restricted'; if (category === 'rate_limited' || category === 'quota_exceeded') return 'rate_limited'; if (category) return 'offline'; if (value && value.idle) return 'idle'; if (value && value.device && value.device.is_restricted) return 'restricted'; return value && value.item ? 'ready' : 'idle' }
function actionAllowed(state) { return state === 'ready' }
function nextPollDelay(options) { if (options && options.category === 'quota_exceeded') return QUOTA_POLL_MS; if (options && options.category === 'rate_limited') return Math.max(POLL_MS, Math.min(60000, Number(options.retry_after_seconds || 0) * 1000 || BACKGROUND_POLL_MS)); return options && options.visible && options.focused ? POLL_MS : BACKGROUND_POLL_MS }
function externalTrackUrl(track) { return track && track.id ? 'https://open.spotify.com/track/' + encodeURIComponent(track.id) : 'https://open.spotify.com/' }
function pageLimit(value) { var number = Number(value); return Math.max(1, Math.min(10, Number.isFinite(number) ? number : 10)) }
function boundedOffset(value) { return Math.max(0, Number(value) || 0) }
function contentState(value, error) { if (error) return 'error'; if (!value) return 'skeleton'; var items = Array.isArray(value) ? value : value.items; return Array.isArray(items) && !items.length ? 'empty' : 'ready' }
function safeArtist(value) { return { id: value && value.id || null, name: value && value.name || '', uri: value && value.uri || null } }
function safeTrack(value) { var raw = value || {}; return { id: raw.id || null, uri: raw.uri || null, name: raw.name || '', explicit: !!raw.explicit, artists: Array.isArray(raw.artists) ? raw.artists.map(safeArtist) : [], album: raw.album || {} } }
function normalizeSearchResults(value) { var raw = value || {}; function items(kind, map) { var page = raw[kind] || {}; return Array.isArray(page.items) ? page.items.map(map) : [] } return { tracks: items('tracks', safeTrack), artists: items('artists', safeArtist), albums: items('albums', function (album) { return { id: album && album.id || null, name: album && album.name || '', images: album && album.images || [], artists: Array.isArray(album && album.artists) ? album.artists.map(safeArtist) : [] } }), playlists: items('playlists', function (playlist) { return { id: playlist && playlist.id || null, name: playlist && playlist.name || '', owner: playlist && playlist.owner || {}, images: playlist && playlist.images || [], external_urls: playlist && playlist.external_urls || {} } }) } }
function isOwnedPlaylist(playlist, userId) { return !!(playlist && playlist.owner && userId && playlist.owner.id === userId) }
function shouldLoadMore(page, backoff) { return !!(page && page.next) && !(Number(backoff) > 0) }
function contentError(error) { return apiCategory(error) || 'unavailable' }
function deviceUiState(error) { var category = apiCategory(error); return category === 'premium_required' ? 'free_read_only' : category === 'no_active_device' ? 'no_device' : category === 'restricted_device' ? 'restricted' : category === 'rate_limited' || category === 'quota_exceeded' ? 'rate_limited' : category ? 'offline' : 'ready' }
function canTransferDevice(device, error) { return deviceUiState(error) === 'ready' && !!(device && device.can_transfer) }
function canAdjustDeviceVolume(device, error) { return deviceUiState(error) === 'ready' && !!(device && device.can_adjust_volume) }
function retryAfterDelay(error) { var seconds = Number(error && (error.retry_after_seconds || error.detail && error.detail.retry_after_seconds)); return Math.max(0, Math.min(60000, Number.isFinite(seconds) ? seconds * 1000 : 0)) }
// F2: no_active_device recovery — open the Spotify desktop app via the
// curated spotify: scheme (allowed by PluginOs.openExternal), poll /devices
// for a bounded 20s, transfer playback to the first transferable device, and
// hand the device id back so the caller retries its original action once.
var DEVICE_WAIT_MS = 20000
var DEVICE_POLL_MS = 1000
function pollForDevice(deadline, pollMs) {
  return restRef('/devices', { method: 'GET' }).then(function (payload) {
    var devices = payload && Array.isArray(payload.devices) ? payload.devices : []
    for (var i = 0; i < devices.length; i++) if (devices[i] && devices[i].can_transfer) return devices[i]
    if (Date.now() >= deadline) return null
    return new Promise(function (resolve) { setTimeout(function () { resolve(pollForDevice(deadline, pollMs)) }, pollMs) })
  }, function () {
    if (Date.now() >= deadline) return null
    return new Promise(function (resolve) { setTimeout(function () { resolve(pollForDevice(deadline, pollMs)) }, pollMs) })
  })
}
function recoverNoDevice(options) {
  var waitMs = options && Number(options.waitMs) > 0 ? Number(options.waitMs) : DEVICE_WAIT_MS
  var pollMs = options && Number(options.pollMs) > 0 ? Number(options.pollMs) : DEVICE_POLL_MS
  if (!osRef || !osRef.openExternal || !restRef) return Promise.resolve(null)
  var opened = osRef.openExternal('spotify:')
  var launch = opened && typeof opened.catch === 'function' ? opened : Promise.resolve()
  return launch.catch(function () {}).then(function () { return pollForDevice(Date.now() + waitMs, pollMs) }).then(function (device) {
    if (!device || !restRef) return null
    return mutateRest('/transfer', 'POST', { device_id: device.id, play: true }).then(function () { return device.id }, function () { return null })
  })
}
function spotifyUrl(kind, item) { return item && item.id ? 'https://open.spotify.com/' + kind + '/' + encodeURIComponent(item.id) : 'https://open.spotify.com/' }
// F2: shared play flow — on typed no_active_device failure, run the recovery
// (open spotify:, bounded device poll, transfer) and retry the play once.
function playWithDeviceRecovery(body, setStatus) {
  function attempt() { return mutateRest('/playback/play', 'POST', body) }
  return attempt().catch(function (error) {
    if (apiCategory(error) !== 'no_active_device') throw error
    setStatus('opening')
    return recoverNoDevice().then(function (deviceId) {
      if (!deviceId) { var none = new Error('no active device after recovery'); none.category = 'no_active_device'; throw none }
      return attempt()
    })
  }).then(function () { return getPlayback() })
}
function useResource(path) { var valueState = useState(null); var value = valueState[0]; var setValue = valueState[1]; var errorState = useState(null); var error = errorState[0]; var setError = errorState[1]; var reloadState = useState(0); var reloadToken = reloadState[0]; var reload = function () { reloadState[1](function (n) { return n + 1 }) }; useEffect(function () { var alive = true; if (!path || !restRef) return function () {}; restRef(path, { method: 'GET' }).then(function (payload) { if (alive) { setValue(payload); setError(null) } }, function (reason) { if (alive) setError(reason) }); return function () { alive = false } }, [path, reloadToken]); return { value: value, error: error, reload: reload } }
function fmtMs(ms) { var total = Math.max(0, Math.floor(Number(ms) || 0) / 1000); var m = Math.floor(total / 60); var s = Math.floor(total % 60); return m + ':' + (s < 10 ? '0' : '') + s }

var COMPLIANCE = Object.freeze({ voicePlaybackEnabled: false, productName: 'Hermes for Spotify', attributionLogo: 'https://storage.googleapis.com/pr-newsroom-wp/1/2023/05/Spotify_Full_Logo_RGB_Green.png' })
function spotifyContentUrl(href) { return href || 'https://open.spotify.com/' }

// -- shared style fragments (Hermes theme tokens only) ------------------------
var S = {
  panel: { border: '1px solid var(--ui-stroke-tertiary)', borderRadius: '10px', background: 'var(--ui-surface-background)', padding: '16px' },
  panelTitle: { margin: '0 0 4px', fontSize: '13px', fontWeight: 600, letterSpacing: '0.02em', textTransform: 'uppercase', color: 'var(--ui-text-tertiary)' },
  muted: { color: 'var(--ui-text-tertiary)', fontSize: '12px', lineHeight: 1.5 },
  quietBtn: { border: '1px solid var(--ui-stroke-secondary)', borderRadius: '6px', padding: '4px 10px', fontSize: '12px', color: 'var(--ui-text-secondary)', background: 'transparent', cursor: 'pointer' },
  primaryBtn: { border: '1px solid var(--ui-stroke-primary)', borderRadius: '6px', padding: '6px 14px', fontSize: '13px', fontWeight: 500, color: 'var(--ui-text-primary)', background: 'var(--ui-bg-tertiary)', cursor: 'pointer' },
  // F1: explicit --ui-text-primary input text (Electron default UA color is
  // near-invisible on --ui-bg-editor) — Search, playlist rename, playlist create.
  inputText: { padding: '6px 10px', border: '1px solid var(--ui-stroke-secondary)', borderRadius: '6px', background: 'var(--ui-bg-input, transparent)', color: 'var(--ui-text-primary)', fontSize: '13px' }
}
var INPUT_STYLE_ID = 'spotify-desktop-input-styles'
// F1: ::placeholder cannot be styled inline — inject a scoped stylesheet tag once
// (idempotent across re-renders) keyed to .spotify-input used by all text inputs.
function ensureInputStyles() {
  if (typeof document === 'undefined' || !document.head || document.getElementById(INPUT_STYLE_ID)) return
  var style = document.createElement('style')
  style.id = INPUT_STYLE_ID
  style.textContent = '.spotify-input::placeholder { color: var(--ui-text-tertiary); opacity: 1; }'
  document.head.appendChild(style)
}

function Attribution(props) { var i18n = useI18n(); var t = i18n.t; return jsxs('span', { className: 'spotify-attribution', style: { display: 'inline-flex', gap: '8px', alignItems: 'center', fontSize: '12px', color: 'var(--ui-text-tertiary)', padding: '11px', margin: '-11px' }, children: [jsx('img', { src: COMPLIANCE.attributionLogo, alt: t('playback.attribution'), style: { width: '70px', height: 'auto', maxHeight: '28px', objectFit: 'contain' } }), jsx('a', { href: spotifyContentUrl(props.href), target: '_blank', rel: 'noreferrer', style: { color: 'inherit' }, children: t('playback.open') })] }) }
function ExplicitBadge() { var i18n = useI18n(); return jsx('small', { className: 'spotify-explicit-badge', title: i18n.t('playback.explicit'), style: { flex: '0 0 auto', marginLeft: '6px', border: '1px solid currentColor', borderRadius: '3px', padding: '0 3px', fontSize: '10px', lineHeight: '14px', color: 'var(--ui-text-tertiary)', fontWeight: 500 }, children: 'E' }) }
function CoverArt(props) { var image = props.image; if (!image || !image.url) return null; var size = props.size === 'large' ? 128 : 48; return jsx('img', { className: 'spotify-cover-art', src: image.url, alt: props.alt || '', style: { width: size + 'px', height: size + 'px', borderRadius: props.size === 'large' ? '8px' : '4px', objectFit: 'contain', display: 'block', flex: '0 0 auto' } }) }
function PrivacyNotice() { var i18n = useI18n(); var t = i18n.t; return jsxs('section', { className: 'spotify-privacy-notice', 'aria-label': t('privacy.title'), style: { margin: '0 28px 16px', padding: '12px 14px', border: '1px solid var(--ui-stroke-quaternary)', borderRadius: '8px', color: 'var(--ui-text-secondary)' }, children: [jsx('strong', { children: t('privacy.title') }), jsx('p', { children: t('privacy.summary') }), jsx('p', { children: t('privacy.deletion') })] }) }
function ContentState(props) { var state = contentState(props.value, props.error); if (state === 'skeleton') return jsx('p', { role: 'status', style: S.muted, children: 'Loading music content…' }); if (state === 'empty') return jsx('p', { style: S.muted, children: 'No items are available for this view.' }); if (state === 'error') return jsxs('div', { role: 'alert', style: { display: 'flex', gap: '10px', alignItems: 'center', color: 'var(--ui-text-secondary)' }, children: [jsx('p', { children: 'Music content is temporarily unavailable.' }), jsx('button', { type: 'button', style: S.quietBtn, onClick: props.reload, children: 'Retry' })] }); return props.children }
function TrackRow(props) {
  var track = safeTrack(props.track)
  var queuedState = useState(''); var queued = queuedState[0]; var setQueued = queuedState[1]
  var playState = useState(''); var playStatus = playState[0]; var setPlayStatus = playState[1]
  function addToQueue() {
    if (!track.uri || !restRef || queued === 'loading') return
    setQueued('loading')
    mutateRest('/queue', 'POST', { uri: track.uri }).then(function () {
      setQueued('added')
    }, function (error) { setQueued(deviceUiState(error)) })
  }
  function playTrack() {
    if (!track.uri || !restRef || playStatus === 'loading' || playStatus === 'opening') return
    setPlayStatus('loading')
    playWithDeviceRecovery({ uris: [track.uri] }, setPlayStatus).then(function () {
      setPlayStatus('ready')
    }, function (error) { setPlayStatus(deviceUiState(error)) })
  }
  var playBlocked = playStatus === 'free_read_only' || playStatus === 'no_device' || playStatus === 'restricted' || playStatus === 'rate_limited' || playStatus === 'offline'
  var playLabel = playStatus === 'opening' ? 'Opening Spotify…' : playStatus === 'loading' ? 'Starting…' : playStatus === 'free_read_only' ? 'Premium required' : playStatus === 'no_device' ? 'Open Spotify on a device' : playStatus === 'restricted' ? 'Device restricted' : playStatus === 'rate_limited' ? 'Try again later' : playStatus === 'offline' ? 'Playback unavailable' : 'Play'
  var queueLabel = queued === 'added' ? 'Added to queue' : queued === 'loading' ? 'Adding…' : queued === 'free_read_only' ? 'Premium required' : queued === 'no_device' ? 'Open Spotify on a device' : queued === 'restricted' ? 'Device restricted' : queued === 'rate_limited' ? 'Try again later' : queued === 'offline' ? 'Queue unavailable' : 'Add to queue'
  return jsxs('li', { style: { display: 'flex', alignItems: 'center', gap: '12px', padding: '8px 4px', borderBottom: '1px solid var(--ui-stroke-quaternary)' }, children: [
    jsx(CoverArt, { image: track.album && track.album.images && track.album.images[0], alt: track.album && track.album.name || track.name, size: 'small' }),
    jsxs('span', { style: { display: 'flex', flexDirection: 'column', gap: '2px', minWidth: 0, flex: 1 }, children: [jsxs('span', { style: { display: 'inline-flex', alignItems: 'center', fontWeight: 500, color: 'var(--ui-text-primary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }, children: [jsx('span', { children: track.name || 'Untitled track' }), track.explicit && jsx(ExplicitBadge, {})] }), jsx('span', { style: { color: 'var(--ui-text-tertiary)', fontSize: '12px', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }, children: track.artists.map(function (artist) { return artist.name }).filter(Boolean).join(', ') })] }),
    jsxs('span', { style: { display: 'inline-flex', gap: '8px', alignItems: 'center', flex: '0 0 auto' }, children: [
      !props.readOnly && jsx('button', { type: 'button', style: S.primaryBtn, disabled: !track.uri || playStatus === 'loading' || playStatus === 'opening' || playBlocked, onClick: playTrack, children: playLabel }),
      !props.readOnly && jsx('button', { type: 'button', style: S.quietBtn, disabled: !track.uri || queued === 'loading' || queued === 'free_read_only' || queued === 'no_device' || queued === 'restricted' || queued === 'rate_limited' || queued === 'offline', onClick: addToQueue, children: queueLabel })
    ] })
  ] })
}
function DevicePicker() { var devices = useResource('/devices'); var mutationState = useState(null); var mutationError = mutationState[0]; var setMutationError = mutationState[1]; var items = devices.value && devices.value.devices || []; function mutate(path, body) { if (!restRef) return; setMutationError(null); mutateRest(path, 'POST', body).then(function () { devices.reload() }, function (error) { setMutationError(error); var delay = retryAfterDelay(error); if (delay) setTimeout(devices.reload, delay) }) } var state = deviceUiState(mutationError || devices.error); var message = state === 'free_read_only' ? 'Spotify Premium is required to control a device.' : state === 'no_device' ? 'Open Spotify on a device, then refresh this list.' : state === 'restricted' ? 'This Spotify device restricts remote controls.' : state === 'rate_limited' ? 'Device updates are delayed to respect Spotify rate limits.' : state === 'offline' ? 'Devices are temporarily unavailable.' : null; return jsxs('aside', { className: 'spotify-device-picker', 'aria-label': 'Spotify devices', style: S.panel, children: [jsx('h2', { style: S.panelTitle, children: 'Devices' }), message && jsx('p', { role: 'status', style: S.muted, children: message }), jsx(ContentState, { value: devices.value && { items: items }, error: devices.error, reload: devices.reload, children: jsxs('div', { children: [jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none', display: 'flex', flexDirection: 'column', gap: '4px' }, children: items.map(function (device, index) { return jsxs('li', { style: { padding: '8px 10px', borderRadius: '8px', background: device.is_active ? 'var(--ui-bg-tertiary)' : 'transparent', border: '1px solid ' + (device.is_active ? 'var(--ui-stroke-secondary)' : 'transparent') }, children: [jsxs('div', { style: { display: 'flex', justifyContent: 'space-between', alignItems: 'baseline', gap: '8px' }, children: [jsxs('strong', { style: { fontSize: '13px', fontWeight: 600, color: 'var(--ui-text-primary)', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }, children: [device.name || 'Unnamed device', device.is_active && jsx('span', { style: { marginLeft: '6px', fontSize: '11px', fontWeight: 500, color: 'var(--ui-text-secondary)' }, children: ' · Current' })] }), jsx('span', { style: { flex: '0 0 auto', fontSize: '12px', color: 'var(--ui-text-tertiary)' }, children: device.type || 'Spotify Connect' })] }), device.is_restricted && jsx('small', { style: { color: 'var(--ui-text-tertiary)' }, children: 'Restricted' }), jsxs('div', { style: { display: 'flex', gap: '8px', marginTop: '6px', alignItems: 'center' }, children: [jsx('button', { type: 'button', style: S.quietBtn, disabled: device.is_active || !canTransferDevice(device, mutationError), onClick: function () { mutate('/transfer', { device_id: device.id, play: true }) }, children: device.is_active ? 'Current device' : 'Transfer here' }), canAdjustDeviceVolume(device, mutationError) && jsx('input', { type: 'range', min: 0, max: 100, value: device.volume_percent, 'aria-label': 'Volume for ' + (device.name || 'device'), style: { flex: 1, minWidth: 0 }, onChange: function (event) { mutate('/playback/volume', { device_id: device.id, volume_percent: Number(event.target.value) }) } })] })] }, device.id || index) }) }), jsx('button', { type: 'button', style: S.quietBtn, onClick: devices.reload, children: 'Refresh devices' }), jsx('p', { style: S.muted, children: "Don't see your device? Open Spotify on it, then refresh." })] }) })] }) }
function QueuePanel() { var queue = useResource('/queue'); var value = queue.value || {}; var upcoming = Array.isArray(value.queue) ? value.queue : []; var category = contentError(queue.error); var message = category === 'premium_required' ? 'Spotify Premium is required to view or add to the queue.' : category === 'no_active_device' ? 'Open Spotify on a device to view its queue.' : category === 'restricted_device' ? 'This device restricts queue access.' : category === 'rate_limited' || category === 'quota_exceeded' ? 'Queue refresh is delayed to respect Spotify rate limits.' : null; return jsxs('aside', { className: 'spotify-queue-panel', 'aria-label': 'Spotify queue', style: S.panel, children: [jsx('h2', { style: S.panelTitle, children: 'Queue' }), message && jsx('p', { role: 'status', style: S.muted, children: message }), jsxs('section', { children: [jsx('h3', { style: { margin: '0 0 4px', fontSize: '13px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: 'Now playing' }), value.currently_playing ? jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none' }, children: jsx(TrackRow, { track: value.currently_playing, readOnly: true }) }) : jsx('p', { style: S.muted, children: 'Nothing is playing.' })] }), jsxs('section', { children: [jsx('h3', { style: { margin: '12px 0 4px', fontSize: '13px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: 'Next up' }), jsx(ContentState, { value: queue.value && { items: upcoming }, error: queue.error, reload: queue.reload, children: jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none' }, children: upcoming.map(function (track, index) { return jsx(TrackRow, { track: track, readOnly: true }, track && track.id || index) }) }) })] }), jsx('p', { style: S.muted, children: 'The queue is read-only here. Add tracks from a music track row.' })] }) }
// F2: context playback (album/playlist context_uri) with the same honest
// typed-failure states TrackRow uses for single-track play.
function ContextPlayButton(props) {
  var statusState = useState(''); var status = statusState[0]; var setStatus = statusState[1]
  function play() {
    if (!props.id || !restRef || status === 'loading' || status === 'opening') return
    setStatus('loading')
    playWithDeviceRecovery({ context_uri: contextUri(props.kind, props.id) }, setStatus).then(function () { setStatus('ready') }, function (error) { setStatus(deviceUiState(error)) })
  }
  var blocked = status === 'free_read_only' || status === 'no_device' || status === 'restricted' || status === 'rate_limited' || status === 'offline'
  var label = status === 'opening' ? 'Opening Spotify…' : status === 'loading' ? 'Starting…' : status === 'free_read_only' ? 'Premium required' : status === 'no_device' ? 'Open Spotify on a device' : status === 'restricted' ? 'Device restricted' : status === 'rate_limited' ? 'Try again later' : status === 'offline' ? 'Playback unavailable' : (props.label || 'Play')
  return jsx('button', { type: 'button', style: S.primaryBtn, disabled: !props.id || status === 'loading' || status === 'opening' || blocked, onClick: play, children: label })
}
function HomeView(props) { var recent = useResource('/home/recently-played?limit=10'); var playlists = useResource('/playlists?limit=10'); var recentItems = recent.value && recent.value.items || []; var playlistItems = playlists.value && playlists.value.page && playlists.value.page.items || []; return jsxs('div', { style: { display: 'flex', flexDirection: 'column', gap: '20px' }, children: [jsxs('section', { children: [jsx('h3', { style: { margin: '0 0 8px', fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: 'Recently played' }), jsx(ContentState, { value: recent.value && { items: recentItems }, error: recent.error, reload: recent.reload, children: jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none' }, children: recentItems.map(function (item, index) { return jsx(TrackRow, { track: item.track }, item.track && item.track.id || index) }) }) })] }), jsxs('section', { children: [jsx('h3', { style: { margin: '0 0 8px', fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: 'Your playlists' }), jsx(ContentState, { value: playlists.value && { items: playlistItems }, error: playlists.error, reload: playlists.reload, children: jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none', display: 'flex', flexDirection: 'column', gap: '2px' }, children: playlistItems.map(function (item, index) { return jsxs('li', { children: [jsx('button', { type: 'button', style: { border: 'none', background: 'transparent', padding: 0, cursor: 'pointer', color: 'var(--ui-text-primary)', textDecoration: 'underline', textDecorationColor: 'var(--ui-stroke-tertiary)', fontWeight: 500, fontSize: '13px' }, onClick: function () { props.onOpenPlaylist && props.onOpenPlaylist(item.id) }, children: item.name || 'Untitled playlist' }), jsx(ContextPlayButton, { kind: 'playlist', id: item.id, label: 'Play playlist' }), jsx('span', { style: { marginLeft: '8px', color: 'var(--ui-text-tertiary)', fontSize: '12px' }, children: 'Open in Spotify' })] }, item.id || index) }) }) })] }), jsx('p', { style: S.muted, children: 'Top items are unavailable until the existing Hermes Spotify authorization includes user-top-read.' })] }) }
function SearchView(props) { var queryState = useState(''); var query = queryState[0]; var setQuery = queryState[1]; var offsetState = useState(0); var offset = offsetState[0]; var setOffset = offsetState[1]; var page = useResource(query ? '/search?q=' + encodeURIComponent(query) + '&offset=' + boundedOffset(offset) + '&limit=10' : null); var results = normalizeSearchResults(page.value && page.value.results); var backoff = page.error && page.error.retry_after_seconds; function section(title, items, kind) { return jsxs('section', { children: [jsx('h3', { style: { margin: '16px 0 8px', fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: title }), jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none' }, children: items.map(function (item, index) { return kind === 'track' ? jsx(TrackRow, { track: item }, item.id || index) : jsxs('li', { style: { display: 'flex', gap: '8px', alignItems: 'center', flexWrap: 'wrap' }, children: [jsx('button', { type: 'button', style: { border: 'none', background: 'transparent', padding: 0, cursor: 'pointer', color: 'var(--ui-text-primary)', textDecoration: 'underline', textDecorationColor: 'var(--ui-stroke-tertiary)', fontWeight: 500, fontSize: '13px' }, onClick: function () { var open = kind === 'album' ? props.onOpenAlbum : kind === 'playlist' ? props.onOpenPlaylist : null; open && open(item.id) }, children: item.name || 'Untitled' }), jsx(ContextPlayButton, { kind: kind, id: item.id, label: kind === 'album' ? 'Play album' : 'Play playlist' })] }, item.id || index) }) })] }) } return jsxs('div', { children: [jsx('h2', { style: { margin: '0 0 12px', fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: 'Search' }), jsx('input', { value: query, placeholder: 'Search Spotify', 'aria-label': 'Search Spotify', className: 'spotify-input', style: Object.assign({}, S.inputText, { width: '100%', maxWidth: '420px', outline: 'none' }), onChange: function (event) { setQuery(event.target.value); setOffset(0) } }), !query ? jsx('p', { style: Object.assign({}, S.muted, { marginTop: '12px' }), children: 'Search tracks, artists, albums, and playlists.' }) : jsx(ContentState, { value: page.value && { items: results.tracks.concat(results.artists, results.albums, results.playlists) }, error: page.error, reload: page.reload, children: jsxs('div', { children: [section('Tracks', results.tracks, 'track'), section('Artists', results.artists, 'artist'), section('Albums', results.albums, 'album'), section('Playlists', results.playlists, 'playlist'), shouldLoadMore(page.value && page.value.results && (page.value.results.tracks || page.value.results.artists), backoff) && jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { setOffset(boundedOffset(offset) + 10) }, children: 'Load more' })] }) })] }) }
function AlbumView(props) { var album = useResource(props.albumId ? '/albums/' + encodeURIComponent(props.albumId) : null); var value = album.value && album.value.album; var tracks = value && value.tracks && value.tracks.items || []; return jsxs('div', { children: [jsxs('div', { style: { display: 'flex', gap: '8px', alignItems: 'center', margin: '0 0 8px' }, children: [props.onBack && jsx('button', { type: 'button', style: S.quietBtn, onClick: props.onBack, children: '← Back' }), jsx('h2', { style: { margin: 0, fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: value && value.name || 'Album' }), value && value.id && jsx(ContextPlayButton, { kind: 'album', id: value.id, label: 'Play album' })] }), jsx(ContentState, { value: value && { items: tracks }, error: album.error, reload: album.reload, children: jsxs('div', { children: [jsx(Attribution, { href: spotifyUrl('album', value) }), jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none' }, children: tracks.map(function (track, index) { return jsx(TrackRow, { track: track }, track.id || index) }) })] }) })] }) }
function PlaylistView(props) { var playlist = useResource(props.playlistId ? '/playlists/' + encodeURIComponent(props.playlistId) : null); var value = playlist.value && playlist.value.playlist; var owned = !!(value && value.is_owned); var tracks = owned && value && value.tracks && value.tracks.items || []; return jsxs('div', { children: [jsxs('div', { style: { display: 'flex', gap: '8px', alignItems: 'center', margin: '0 0 8px' }, children: [props.onBack && jsx('button', { type: 'button', style: S.quietBtn, onClick: props.onBack, children: '← Back' }), jsx('h2', { style: { margin: 0, fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: value && value.name || 'Playlist' }), value && value.id && jsx(ContextPlayButton, { kind: 'playlist', id: value.id, label: 'Play playlist' })] }), jsx(ContentState, { value: value && { items: owned ? tracks : [value] }, error: playlist.error, reload: playlist.reload, children: jsxs('div', { children: [jsx(Attribution, { href: spotifyUrl('playlist', value) }), owned ? jsxs('div', { children: [jsx(PlaylistManager, { playlist: value, owned: owned, onResync: playlist.reload }), jsx('ul', { style: { margin: 0, padding: 0, listStyle: 'none' }, children: tracks.map(function (entry, index) { return jsx(TrackRow, { track: entry.track || entry }, entry.track && entry.track.id || index) }) })] }) : jsx('p', { style: S.muted, children: 'This playlist belongs to another user, so this companion shows metadata only.' })] }) })] }) }
function libraryContainsPath(kind, ids) { return '/library/contains?kind=' + encodeURIComponent(kind) + ids.map(function (id) { return '&ids=' + encodeURIComponent(id) }).join('') }
function containsSaved(payload, index) { return !!(payload && Array.isArray(payload.contains) && payload.contains[index]) }
function snapshotAfter(previous, response) { return response && response.snapshot_id || previous || null }
function HeartButton(props) { var contains = useResource(props.id ? libraryContainsPath(props.kind, [props.id]) : null); var saved = containsSaved(contains.value, 0); var mutationState = useState(false); var mutating = mutationState[0]; var setMutating = mutationState[1]; var errorState = useState(null); var mutationError = errorState[0]; var setMutationError = errorState[1]; function toggle() { if (!props.id || !restRef || !contains.value || mutating) return; setMutating(true); setMutationError(null); mutateRest('/library/items', 'POST', { kind: props.kind, ids: [props.id], saved: !saved }).then(function () { contains.reload(); if (props.onResync) props.onResync() }, function (error) { setMutationError(error); var delay = retryAfterDelay(error); if (delay) setTimeout(contains.reload, delay) }).then(function () { setMutating(false) }) } var unavailable = deviceUiState(mutationError || contains.error); return jsx('button', { type: 'button', style: S.quietBtn, disabled: !props.id || !contains.value || mutating || unavailable !== 'ready', onClick: toggle, 'aria-pressed': saved, children: saved ? 'Saved' : mutating ? 'Updating…' : 'Save' }) }
function LibraryView(props) { var filterState = useState('playlists'); var filter = filterState[0]; var setFilter = filterState[1]; var layoutState = useState('grid'); var layout = layoutState[0]; var setLayout = layoutState[1]; var kind = filter === 'playlists' ? 'playlists' : filter; var resource = useResource(filter === 'playlists' ? '/playlists?limit=10' : '/library/' + kind + '?limit=10'); var page = resource.value && (resource.value.page || resource.value) || {}; var items = Array.isArray(page.items) ? page.items : []; useEffect(function () { if (storageRef && storageRef.get) { var value = storageRef.get('layout'); if (value === 'grid' || value === 'list') setLayout(value) } }, []); function choose(next) { setFilter(next); resource.reload() } function chooseLayout(next) { setLayout(next); if (storageRef && storageRef.set) storageRef.set('layout', next) } return jsxs('div', { children: [jsx('h2', { style: { margin: '0 0 12px', fontSize: '15px', fontWeight: 600, color: 'var(--ui-text-primary)' }, children: 'Library' }), filter === 'playlists' && jsx(PlaylistCreator, { onResync: resource.reload }), jsxs('div', { role: 'tablist', style: { display: 'inline-flex', gap: '4px', margin: '12px 0', padding: '3px', border: '1px solid var(--ui-stroke-tertiary)', borderRadius: '8px' }, children: [['playlists', 'Playlists'], ['albums', 'Albums'], ['artists', 'Artists']].map(function (item) { return jsx('button', { type: 'button', 'aria-pressed': filter === item[0], onClick: function () { choose(item[0]) }, style: { border: 'none', borderRadius: '6px', padding: '4px 12px', fontSize: '12px', cursor: 'pointer', background: filter === item[0] ? 'var(--ui-bg-tertiary)' : 'transparent', color: filter === item[0] ? 'var(--ui-text-primary)' : 'var(--ui-text-secondary)', fontWeight: filter === item[0] ? 600 : 400 }, children: item[1] }, item[0]) }) }), jsxs('div', { role: 'group', 'aria-label': 'Library layout', style: { display: 'inline-flex', gap: '4px', marginLeft: '12px' }, children: [jsx('button', { type: 'button', style: S.quietBtn, 'aria-pressed': layout === 'grid', onClick: function () { chooseLayout('grid') }, children: 'Grid' }), jsx('button', { type: 'button', style: S.quietBtn, 'aria-pressed': layout === 'list', onClick: function () { chooseLayout('list') }, children: 'List' })] }), jsx(ContentState, { value: resource.value && { items: items }, error: resource.error, reload: resource.reload, children: jsx('ul', { style: { display: layout === 'grid' ? 'grid' : 'block', gridTemplateColumns: layout === 'grid' ? 'repeat(auto-fill, minmax(180px, 1fr))' : undefined, gap: '12px', padding: 0, listStyle: 'none' }, children: items.map(function (item, index) { var id = item && item.id || null; var label = item && item.name || 'Untitled'; var itemKind = filter === 'albums' ? 'album' : filter === 'artists' ? 'artist' : null; var openable = filter === 'playlists' ? props.onOpenPlaylist : filter === 'albums' ? props.onOpenAlbum : null; return jsxs('li', { style: { listStyle: 'none', border: '1px solid var(--ui-stroke-quaternary)', borderRadius: '8px', padding: '10px', background: 'var(--ui-bg-quinary)' }, children: [jsx('strong', { style: { display: 'block', marginBottom: '6px', fontSize: '13px', fontWeight: 500, color: 'var(--ui-text-primary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }, children: label }), jsxs('div', { style: { display: 'flex', gap: '6px', alignItems: 'center', flexWrap: 'wrap' }, children: [openable && jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { openable(id) }, disabled: !id, children: filter === 'playlists' ? 'Open playlist' : 'Open album' }), filter !== 'artists' && jsx(ContextPlayButton, { kind: filter === 'albums' ? 'album' : 'playlist', id: id, label: filter === 'albums' ? 'Play album' : 'Play playlist' }), itemKind && jsx(HeartButton, { kind: itemKind, id: id, onResync: resource.reload })] }), filter === 'playlists' && jsx('a', { href: spotifyUrl('playlist', item), target: '_blank', rel: 'noreferrer', style: { marginLeft: '0', marginTop: '6px', display: 'inline-block', fontSize: '12px', color: 'var(--ui-text-secondary)' }, children: 'Open in Spotify' })] }, id || index) }) }) })] }) }
function PlaylistManager(props) { var snapshotState = useState(props.playlist && props.playlist.snapshot_id || null); var snapshot = snapshotState[0]; var setSnapshot = snapshotState[1]; var nameState = useState(props.playlist && props.playlist.name || ''); var name = nameState[0]; var setName = nameState[1]; var errorState = useState(null); var error = errorState[0]; var setError = errorState[1]; var tracks = props.playlist && props.playlist.tracks && props.playlist.tracks.items || []; function resync(response) { setSnapshot(function (old) { return snapshotAfter(old, response) }); props.onResync() } function mutate(body) { if (!props.owned || !restRef) return; setError(null); mutateRest('/playlists/' + encodeURIComponent(props.playlist.id) + '/items', 'POST', body).then(resync, function (reason) { setError(reason); var delay = retryAfterDelay(reason); if (delay) setTimeout(props.onResync, delay) }) } function rename() { if (!props.owned || !name || !restRef) return; mutateRest('/playlists/' + encodeURIComponent(props.playlist.id), 'PATCH', { name: name }).then(resync, setError) } if (!props.owned) return null; var first = tracks[0] && (tracks[0].track || tracks[0]); return jsxs('div', { style: { display: 'flex', flexWrap: 'wrap', gap: '8px', alignItems: 'center', margin: '0 0 12px' }, children: [jsx('input', { value: name, 'aria-label': 'Playlist name', className: 'spotify-input', style: S.inputText, onChange: function (event) { setName(event.target.value) } }), jsx('button', { type: 'button', style: S.quietBtn, onClick: rename, disabled: !name, children: 'Rename playlist' }), jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { first && first.uri && mutate({ action: 'add', uris: [first.uri] }) }, disabled: !first || !first.uri, children: 'Add first item' }), jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { first && first.uri && mutate({ action: 'remove', uris: [first.uri], snapshot_id: snapshot }) }, disabled: !first || !first.uri || !snapshot, children: 'Remove first item' }), jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { mutate({ action: 'reorder', range_start: 0, insert_before: 1, snapshot_id: snapshot }) }, disabled: !snapshot, children: 'Move first item down' }), jsx('p', { role: 'status', style: { flexBasis: '100%', margin: 0 }, children: error && (deviceUiState(error) === 'rate_limited' ? 'Playlist changes are delayed to respect Spotify rate limits.' : 'Playlist change failed; contents will be refreshed.') })] }) }
function PlaylistCreator(props) { var nameState = useState(''); var name = nameState[0]; var setName = nameState[1]; var busyState = useState(false); var busy = busyState[0]; var setBusy = busyState[1]; function create() { if (!restRef || !name || busy) return; setBusy(true); mutateRest('/playlists', 'POST', { name: name }).then(function () { setName(''); props.onResync() }).then(function () { setBusy(false) }) } return jsxs('div', { style: { display: 'flex', gap: '8px', alignItems: 'center', margin: '0 0 12px' }, children: [jsx('input', { value: name, placeholder: 'New playlist name', 'aria-label': 'New playlist name', className: 'spotify-input', style: S.inputText, onChange: function (event) { setName(event.target.value) } }), jsx('button', { type: 'button', style: S.primaryBtn, disabled: !name || busy, onClick: create, children: busy ? 'Creating…' : 'Create playlist' })] }) }
function ContentWorkspace() { var viewState = useState('home'); var view = viewState[0]; var setView = viewState[1]; var selectionState = useState(null); var selection = selectionState[0]; var setSelection = selectionState[1]; var i18n = useI18n(); var t = i18n.t; function choose(next) { setSelection(null); setView(next) } function openAlbum(id) { if (!id) return; setSelection(id); setView('album') } function openPlaylist(id) { if (!id) return; setSelection(id); setView('playlist') } var tabs = [['home', t('music.home')], ['search', t('music.search')], ['library', t('music.library')]]; var body = view === 'library' ? jsx(LibraryView, { onOpenAlbum: openAlbum, onOpenPlaylist: openPlaylist }) : view === 'search' ? jsx(SearchView, { onOpenAlbum: openAlbum, onOpenPlaylist: openPlaylist }) : view === 'album' ? jsx(AlbumView, { albumId: selection, onBack: function () { choose('library') } }) : view === 'playlist' ? jsx(PlaylistView, { playlistId: selection, onBack: function () { choose('library') } }) : jsx(HomeView, { onOpenPlaylist: openPlaylist }); return jsxs('section', { style: { display: 'flex', flexDirection: 'column', gap: '16px', minWidth: 0 }, children: [jsxs('div', { role: 'tablist', style: { display: 'inline-flex', gap: '4px', padding: '3px', border: '1px solid var(--ui-stroke-tertiary)', borderRadius: '8px', alignSelf: 'flex-start' }, children: tabs.map(function (tab) { return jsx('button', { type: 'button', 'aria-pressed': view === tab[0], onClick: function () { choose(tab[0]) }, style: { border: 'none', borderRadius: '6px', padding: '5px 14px', fontSize: '13px', cursor: 'pointer', background: view === tab[0] ? 'var(--ui-bg-tertiary)' : 'transparent', color: view === tab[0] ? 'var(--ui-text-primary)' : 'var(--ui-text-secondary)', fontWeight: view === tab[0] ? 600 : 400 }, children: tab[1] }, tab[0]) }) }), body] }) }

function ConnectionNotice() {
  var i18n = useI18n(); var t = i18n.t
  var phaseState = useState('loading'); var phase = phaseState[0]; var setPhase = phaseState[1]
  var disconnectedState = useState(false); var disconnected = disconnectedState[0]; var setDisconnected = disconnectedState[1]
  useEffect(function () { var active = true; var timer; function poll() { if (disconnected) return; getStatus().then(function (status) { if (!active) return; var next = connectionState(status, false); setPhase(function (current) { return current === 'connecting' ? transitionConnection(current, next === 'connected' ? 'status_connected' : next === 'expired' ? 'status_expired' : 'status_not_authenticated') : next }) }, function () { if (active) setPhase('error') }) } poll(); timer = setInterval(poll, POLL_MS); return function () { active = false; clearInterval(timer) } }, [disconnected])
  function connect() { setDisconnected(false); setPhase(function (current) { return transitionConnection(current, 'connect_opened') }); if (osRef && osRef.openExternal) osRef.openExternal(AUTH_SETTINGS_DEEP_LINK).catch(function () { setPhase('error') }); else setPhase('error') }
  if (phase === 'connected') return jsx('section', { className: 'spotify-connection', style: { display: 'flex', justifyContent: 'flex-end', padding: '0 28px 8px' }, children: jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { clearPluginData(storageRef); setDisconnected(true); setPhase('disconnected') }, children: t('connection.delete') }) })
  var key = phase === 'login_required' ? 'connection.login' : phase === 'connecting' ? 'connection.waiting' : phase === 'expired' ? 'connection.expired' : phase === 'disconnected' ? 'connection.disconnected' : phase === 'error' ? 'connection.error' : 'connection.checking'
  var action = (phase === 'login_required' || phase === 'expired' || phase === 'disconnected') ? jsx('button', { type: 'button', style: S.primaryBtn, onClick: connect, children: t('connection.connect') }) : phase === 'error' ? jsx('button', { type: 'button', style: S.quietBtn, onClick: function () { setPhase('loading') }, children: t('connection.retry') }) : null
  return jsxs('section', { className: 'spotify-connection', style: { display: 'flex', gap: '12px', alignItems: 'center', justifyContent: 'space-between', margin: '0 28px 8px', padding: '10px 14px', border: '1px solid var(--ui-stroke-quaternary)', borderRadius: '8px', background: 'var(--ui-bg-quinary)' }, children: [jsx('p', { role: 'status', style: { margin: 0, fontSize: '13px', color: 'var(--ui-text-secondary)' }, children: t(key) }), (phase === 'login_required' || phase === 'connecting' || phase === 'expired' || phase === 'disconnected') && jsx('p', { style: Object.assign({}, S.muted, { margin: 0 }), children: t('connection.help') }), action] })
}

function NowPlayingBar() {
  var i18n = useI18n(); var t = i18n.t
  var playbackState = useState(null); var playback = playbackState[0]; var setPlayback = playbackState[1]
  var modeState = useState('idle'); var mode = modeState[0]; var setMode = modeState[1]
  var actionErrorState = useState(null); var actionError = actionErrorState[0]; var setActionError = actionErrorState[1]
  var nowState = useState(Date.now()); var now = nowState[0]; var setNow = nowState[1]
  useEffect(function () { var alive = true; var timer; function poll() { var visible = typeof document === 'undefined' || !document.hidden; var focused = typeof document === 'undefined' || !document.hasFocus || document.hasFocus(); getPlayback().then(function (value) { if (!alive) return; setPlayback(value); setMode(playbackUiState(value)); timer = setTimeout(poll, nextPollDelay({ visible: visible, focused: focused })) }, function (error) { if (!alive) return; var category = apiCategory(error) || 'unavailable'; setMode(playbackUiState({ category: category })); timer = setTimeout(poll, nextPollDelay({ visible: visible, focused: focused, category: category, retry_after_seconds: error && error.retry_after_seconds })) }) } poll(); var tick = setInterval(function () { setNow(Date.now()) }, 1000); return function () { alive = false; clearTimeout(timer); clearInterval(tick) } }, [])
  var item = playback && playback.item; var duration = item && item.duration_ms; var progress = interpolateProgress(playback, now); var disabled = !actionAllowed(mode)
  function transport(action, body) { if (disabled || !restRef) return; setActionError(null); mutateRest('/playback/' + action, 'POST', body || {}).then(function () { return getPlayback() }).then(function (value) { setPlayback(value); setMode(playbackUiState(value)); setActionError(null) }, function (error) { var category = apiCategory(error) || 'unavailable'; setActionError(category); setMode(playbackUiState({ category: category })) }) }
  var messageKey = mode === 'free_read_only' ? 'playback.free' : mode === 'no_device' || mode === 'idle' ? 'playback.empty' : mode === 'restricted' ? 'playback.restricted' : mode === 'rate_limited' ? 'playback.throttled' : mode === 'offline' ? 'playback.offline' : null
  var actionMessage = actionError === 'premium_required' ? t('playback.free') : actionError === 'no_active_device' ? t('playback.empty') : actionError === 'restricted_device' ? t('playback.restricted') : actionError === 'rate_limited' || actionError === 'quota_exceeded' ? t('playback.throttled') : actionError ? t('playback.offline') : null
  var image = item && item.album && item.album.images && item.album.images[0]
  var volume = playback && playback.device && Number(playback.device.volume_percent)
  var canVolume = Number.isFinite(volume)
  var repeat = playback && playback.repeat_state || 'off'
  var nextRepeat = repeat === 'off' ? 'context' : repeat === 'context' ? 'track' : 'off'
  return jsxs('footer', { className: 'spotify-now-playing', style: { borderTop: '1px solid var(--ui-stroke-tertiary)', background: 'var(--ui-bg-editor)', padding: '12px 16px', display: 'grid', gridTemplateColumns: 'minmax(180px, 1fr) auto minmax(220px, 1fr)', gap: '16px', alignItems: 'center' }, children: [
    jsxs('div', { style: { display: 'flex', gap: '12px', alignItems: 'center', minWidth: 0 }, children: [jsx(CoverArt, { image: image, alt: item && item.name, size: 'small' }), item ? jsxs('span', { style: { display: 'flex', flexDirection: 'column', gap: '2px', minWidth: 0 }, children: [jsxs('span', { style: { display: 'inline-flex', alignItems: 'center', fontWeight: 500, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }, children: [jsx('span', { children: item.name }), item.explicit && jsx(ExplicitBadge, {})] }), jsx('span', { style: { color: 'var(--ui-text-tertiary)', fontSize: '12px', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }, children: (item.artists || []).map(function (artist) { return artist.name }).filter(Boolean).join(', ') })] }) : jsx('span', { style: { color: 'var(--ui-text-tertiary)', fontSize: '13px' }, children: messageKey ? t(messageKey) : t('playback.empty') })] }),
    jsxs('div', { role: 'group', 'aria-label': t('playback.play'), style: { display: 'flex', gap: '6px', alignItems: 'center' }, children: [jsx('button', { type: 'button', style: S.quietBtn, disabled: disabled, onClick: function () { transport('previous') }, children: t('playback.previous') }), jsx('button', { type: 'button', style: S.primaryBtn, disabled: disabled, onClick: function () { transport(playback && playback.is_playing ? 'pause' : 'play') }, children: t(playback && playback.is_playing ? 'playback.pause' : 'playback.play') }), jsx('button', { type: 'button', style: S.quietBtn, disabled: disabled, onClick: function () { transport('next') }, children: t('playback.next') })] }),
    jsxs('div', { style: { display: 'flex', gap: '10px', alignItems: 'center', justifyContent: 'flex-end', minWidth: 0, flexWrap: 'wrap' }, children: [item && duration ? jsxs('span', { style: { flex: '0 0 auto', fontSize: '11px', color: 'var(--ui-text-tertiary)', fontVariantNumeric: 'tabular-nums' }, children: [fmtMs(progress), ' / ', fmtMs(duration)] }) : null, jsx('input', { type: 'range', min: 0, max: duration || 0, value: progress, disabled: disabled || !duration, 'aria-label': t('playback.seek'), style: { flex: 1, minWidth: '80px' }, onChange: function (event) { transport('seek', { position_ms: clampSeek(event.target.value, duration) }) } }), jsx('input', { type: 'range', min: 0, max: 100, value: canVolume ? volume : 0, disabled: disabled || !canVolume, 'aria-label': 'Volume', style: { width: '72px' }, onChange: function (event) { transport('volume', { volume_percent: Math.max(0, Math.min(100, Number(event.target.value) || 0)) }) } }), jsx('button', { type: 'button', style: S.quietBtn, disabled: disabled, 'aria-pressed': !!(playback && playback.shuffle_state), onClick: function () { transport('shuffle', { state: !(playback && playback.shuffle_state) }) }, children: 'Shuffle' }), jsx('button', { type: 'button', style: S.quietBtn, disabled: disabled, 'aria-pressed': repeat !== 'off', onClick: function () { transport('repeat', { state: nextRepeat }) }, children: 'Repeat: ' + repeat }), actionMessage && jsx('p', { role: 'alert', style: Object.assign({}, S.muted, { margin: 0, flexBasis: '100%' }), children: actionMessage }), item && jsx(Attribution, { href: externalTrackUrl(item) })] })
  ] })
}
function MusicPage() { var i18n = useI18n(); var t = i18n.t; useEffect(ensureInputStyles, []); return jsxs('main', { className: 'spotify-desktop-page', style: { background: 'var(--ui-bg-secondary)', color: 'var(--ui-text-primary)', fontFamily: 'Inter, sans-serif', minHeight: '100%', display: 'flex', flexDirection: 'column' }, children: [jsx('header', { style: { padding: '24px 28px 4px', display: 'flex', alignItems: 'baseline', gap: '12px' }, children: jsxs('div', { children: [jsx('h1', { style: { margin: 0, fontSize: '20px', fontWeight: 700, letterSpacing: '-0.01em' }, children: t('music.title') }), jsx('p', { style: { margin: '2px 0 0', fontSize: '13px', color: 'var(--ui-text-tertiary)' }, children: t('music.subtitle') })] }) }), jsx(ConnectionNotice, {}), jsx('section', { style: { flex: 1, padding: '12px 28px 20px' }, children: jsxs('div', { style: { display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) minmax(260px, 340px)', gap: '16px' }, children: [jsx(ContentWorkspace, {}), jsxs('div', { style: { display: 'grid', alignContent: 'start', gap: '16px' }, children: [jsx(DevicePicker, {}), jsx(QueuePanel, {})] })] }) }), jsx(NowPlayingBar, {}), jsx(PrivacyNotice, {}), jsx('p', { style: { padding: '0 28px 12px', color: 'var(--ui-text-quaternary)', fontSize: '11px' }, children: t('shell.disconnectHelp') })] }) }

var plugin = { id: ID, name: COMPLIANCE.productName, description: 'Personal Spotify companion route backed by the Hermes plugin API.', defaultEnabled: false, register: function (ctx) { restRef = ctx.rest; osRef = ctx.os; storageRef = ctx.storage; if (ctx.i18n && ctx.i18n.register) ctx.i18n.register(translations); ctx.registerMany([{ id: 'page', area: ROUTES_AREA, data: { path: '/spotify' }, render: function () { return jsx(MusicPage, {}) } }, { id: 'nav', area: SIDEBAR_NAV_AREA, order: 60, data: { codicon: 'music', label: 'Music', path: '/spotify' } }, { id: 'open', area: PALETTE_AREA, data: { id: 'spotify-desktop.open', label: 'Music: Open Spotify companion', keywords: ['music', 'spotify', 'playback'], run: function () { host.navigate('/spotify') } } }]); ctx.onDispose(function () { restRef = null; osRef = null; storageRef = null }) } }

export { AUTH_SETTINGS_DEEP_LINK, COMPLIANCE, actionAllowed, boundedOffset, canAdjustDeviceVolume, canTransferDevice, clampSeek, clearPluginData, connectionState, containsSaved, contentState, contextUri, deviceUiState, ensureInputStyles, interpolateProgress, isOwnedPlaylist, libraryContainsPath, mutateRest, nestMessages, nextPollDelay, normalizeSearchResults, pageLimit, playWithDeviceRecovery, playbackUiState, plugin as default, recoverNoDevice, retryAfterDelay, shouldLoadMore, snapshotAfter, transitionConnection, translations }
